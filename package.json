{
  "name": "phonebook_backend",
  "version": "0.0.1",
  "description": "exercise",
  "main": "index.js",
  "scripts": {
    "build:ui": "shx rm -rf dist && cd ../The-Phonebook && npm run build && shx cp -r dist ../phonebookBackend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"error: no test command added\" && exit 1"
  },
  "author": "Danton",
  "license": "MIT",
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.10.1",
    "morgan": "^1.12.0",
    "shx": "^0.4.0"
  }
}
